/**
 * Ejercicio 1 - Selección por ID y lectura de textContent
 * Conceptos: querySelector, textContent
 *
 * querySelector → busca el primer elemento que coincide con el selector CSS dado
 * textContent   → devuelve el texto plano contenido dentro de un elemento
 */

/*1.Necesitamos verificar que el script se está cargando correctamente.
Localiza el título principal de la página, el que sirve de banner, y
 envía su contenido de texto a la consola para confirmar que tienes acceso a él.*/

console.log(document.querySelector('#titulo-principal').textContent)


